<!DOCTYPE html>
<html lang="es">

<%- include('../components/head', {pageName: 'Editar curso'}) %>

<%- include('../custom/body') %>

  <%- include('../components/navbar') %>

  <main class="container mx-auto">

    <div class="shadow-xl p-10 rounded max-w-6xl mx-auto mt-10 bg-white">
      <h2 class="text-2xl mb-5">Editar '<span class="font-bold"><%= course.name %></span>'</h2>
      <%- include('../components/courseForm', {
        action: `/cursos/editar/${course.id}`, 
        course, 
        create: false}) %>
    </div>

  </main>
  
</body>
</html>